namespace AddressAnalyzer.Common
{
    /// <summary>
    /// Constants.
    /// </summary>
    public static class Constants
    {
        /// <summary>
        /// Used in the failure reasons list when a service is offline.
        /// </summary>
        public const string SERVER_OFFLINE = "Service is offline";
        /// <summary>
        /// Used in the failure reasons list when a service responds with an error code.
        /// </summary>
        public const string ERROR_PREFIX = "Request Error: ";
    }
}
